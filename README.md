# Harness System

This project is the foundation of a harness system for managing agent-driven software tasks. It models sessions, their lifecycle states, and the events produced as an agent plans, reads code, modifies files, runs tests, and progresses toward completion. The goal is to provide a structured, observable, and extensible core for coordinating reliable coding workflows.